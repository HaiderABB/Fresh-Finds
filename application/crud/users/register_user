from sqlalchemy import func
from sqlalchemy.orm import Session
from database.session import get_db


def user_registeration(user_data, db: Session):

    result = db.query(
        func.validate_user(user_data.email).label('user')
    ).all()

    return result
